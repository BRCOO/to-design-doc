---
name: to-design-doc
description: Write a new technical design doc (方案/设计文档) from scratch in the team's terse Chinese style — fixed 6-section skeleton where each section answers one question, every fact stated once, no code blocks, tables only for multi-column comparisons. Manual invocation only.
disable-model-invocation: true
---

# To Design Doc

把对话中的需求写成一份**中文技术方案**。只负责从零写：如果用户贴来一份已有草稿，先从中提取需求与决策，再按本流程重写，不沿用草稿的结构和句子。

## 流程

1. 从对话提取需求要点：做什么、给谁用、输出什么、边界条件。核心决策缺失时最多问一轮；信息够了直接写。
2. 按骨架写。某节没有实质内容就删掉该节，不留空节凑结构。
3. 输出前逐条过「自查清单」，全部通过才交付。

## 骨架

文档以一行功能名标题开头，之后每节只回答一个问题：

| 节 | 回答的问题 |
|---|---|
| 需求 | 要做什么？给谁用？输出什么结果？ |
| 调用位置 | 新逻辑插在现有调用链的哪一步？ |
| 结果使用方式 | 各使用方分别读哪些结果、怎么用？ |
| 处理规则 | 判定规则和边界条件是什么？ |
| 代码改动 | 改哪些文件/函数、各做什么？ |
| 验收标准 | 什么场景下应观察到什么结果？ |

- 「调用位置」用普通文本行写流程（如 `A -> B -> 新逻辑 -> C`），不用代码块围栏，不画图。
- 「代码改动」只写 文件/函数 + 一句话，不写代码。
- 伪代码代码块（若有）只出现在「处理规则」里，仅当比文字更清楚时使用。

## 硬规则

1. 全文无实现代码、结构体/字段定义。允许少量简洁伪代码代码块：只用于比文字更清楚的判定逻辑，尽量短（通常不超过 5 行），不写变量声明与实现细节。字段级细节压成一句话（例："输出结构增加降级事件标志与连续失效计数"）。
2. 同一事实或规则只写一次；发现两节表达同一意思就合并。
3. 表格只用于多列对照信息（场景×期望、选项×利弊、字段×规则）；每条一句话能说清的清单用分点。不为用表格而用表格。
4. 不写「不在范围内」「风险」「备选方案」等凑数节，除非用户明确要求。
5. 篇幅以金标准样例为基准（约半页）；超了先删重复，再删细节。
6. 术语沿用对话与代码库已有词汇，不造新词。

## 自查清单

- [ ] 每节能用一句话说出它回答的问题
- [ ] 没有任何事实出现两次
- [ ] 没有实现代码；伪代码块（若有）简短且比文字更清楚
- [ ] 多列对照信息都用了表格，单句清单都用了分点
- [ ] 没有删掉后不影响信息的句子

## 金标准样例

<example>
传感器失效降级

需求
给感知融合模块新增传感器失效降级：相机、激光雷达、毫米波任一源超时或输出无效时，融合结果改用剩余有效源的组合，并记录一次降级事件；三源全部失效时输出上一帧融合结果并计数。融合结果供预测模块和规划模块使用。

调用位置
感知输入
  -> 源有效性检查（新增）
  -> 降级策略选择（新增）
  -> 多源融合
  -> 预测 / 规划读取结果
有效性检查在融合前完成，只执行一次，不重复判定。

结果使用方式
- 预测模块：读取融合结果做轨迹预测，不感知降级细节。
- 规划模块：读取融合结果做决策规划。
- 监控侧：读取降级事件与连续失效计数，用于诊断。

处理规则
| 失效源 | 有效源组合 | 附加影响 |
|---|---|---|
| 相机 | 激光雷达 + 毫米波 | 无 |
| 激光雷达 | 相机 + 毫米波 | 目标距离精度降一档 |
| 毫米波 | 相机 + 激光雷达 | 无 |
- 任一源超时或输出无效即判定该源失效。
- 任一源恢复有效后，连续失效计数清零。
判定逻辑：
```
if 无有效源:
    输出上一帧融合结果
    连续失效计数 += 1
else:
    按有效源组合融合并触发降级事件
```

代码改动
- 新增源有效性检查，判定三个源是否超时或输出无效。
- 新增降级策略选择器，按有效源组合选择融合分支。
- 修改融合主入口，在融合前插入有效性检查与策略选择。
- 融合输出结构增加降级事件标志与连续失效计数。

验收标准
- 相机失效时，融合结果仅使用激光雷达 + 毫米波，并产生降级事件。
- 激光雷达失效时，融合结果使用相机 + 毫米波，距离精度降一档。
- 毫米波失效时，融合结果使用相机 + 激光雷达。
- 三源全部失效时，输出与上一帧相同，连续失效计数 +1。
- 任一源恢复后，连续失效计数清零。
- 无失效时，融合结果使用三源全量，无降级事件。
</example>
